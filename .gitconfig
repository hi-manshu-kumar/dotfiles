[user]
	name = Himanshu
	email = himanshudas@indiagold.co
[fetch]
	prune = true
[pull]
	rebase = true
[alias]
	prune-no-longer-exist = "!git branch -vv | grep ': gone]' | grep -v '\\*' | awk '{ print $1; }' | xargs -r git branch -d"
	gl = config --global -l
